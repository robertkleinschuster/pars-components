<?php
/**
 * @var Pars\Component\View $view
 * @var string $templateFolder
 */
?>
<!DOCTYPE html>
<html lang="<?= $view->hasData('language') ? $view->getData('language') : 'en' ?> ">
<head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
    <?php if ($view->hasData('description')): ?>
        <meta name="description" content="<?= $view->getData('description') ?>">
    <?php endif; ?>
    <?php if ($view->hasData('author')): ?>
        <meta name="author" content="<?= $view->getData('author') ?>">
    <?php endif; ?>
    <?php if ($view->hasData('title')): ?>
        <title><?= $view->getData('title') ?></title>
    <?php endif; ?>
    <?php if ($view->hasData('favicon')): ?>
        <link rel="shortcut icon" href="<?= $view->getData('favicon') ?>"/>
    <?php else: ?>
        <link rel="shortcut icon" href="favicon.ico"/>
    <?php endif; ?>
    <?= $this->section('stylesheets') ?>

    <style>
        body {
            padding-top: 70px;
        }

        .app {
            min-height: 100vh;
        }

        .app-footer {
            padding-bottom: 1em;
        }

        .navbar-brand {
            padding: 0;
        }

        .navbar-brand img {
            margin: -.5rem 0;
            filter: brightness(0) invert(1);
        }
    </style>
</head>
<body class="app">

<?php if ($view->hasData('title')): ?>
    <h1 class="text-center mx-auto"><?= $view->getData('title') ?></h1>
<?php endif; ?>
<?php if ($view->hasData('heading')): ?>
    <hr>
    <h2 class="text-center mx-auto mb-5"><?= $view->getData('heading') ?></h2>
<?php endif; ?>

<div class="app-content">
    <main class="container">
        <?= $this->section('content') ?>
    </main>
</div>

<footer class="app-footer">
    <div class="container">
        <hr/>
        <?php if ($this->section('footer')): ?>
            <?= $this->section('footer') ?>
        <?php else: ?>
            <p>
                &copy; <?= date('Y') ?> <a href="https://kleinschuster.de">Robert Kleinschuster</a>
            </p>
        <?php endif ?>
    </div>
</footer>
<?= $this->section('javascript') ?>
</body>
</html>
